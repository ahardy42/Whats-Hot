import React from 'react';
import { Link } from 'react-router-dom';
import './NavBar.sass';

const NavBar = ({ isMapView }) => {
    return (
        <nav className="myNav">
        </nav>
    )
}

export default NavBar;